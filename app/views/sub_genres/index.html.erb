<p id="notice"><%= notice %></p>

<div class="container">
  <h1 class="text-center mt-4 mb-4">
    Sub genres
  </h1>

  <%= link_to(
    'New Sub genre',
    new_sub_genre_path,
    class: 'btn btn-outline-primary mt-4 mb-4'
  ) %>

  <table class="table">
    <thead>
      <tr>
        <th scope="col">Title</th>
        <th colspan="3">Actions</th>
      </tr>
    </thead>

    <tbody>
      <% @sub_genres.each do |sub_genre| %>
        <tr>
          <th scope="row"><%= sub_genre.id %></th>

          <td>
            <%= link_to(
              'Show',
              sub_genre,
              class: 'btn btn-sm btn-outline-info'
            ) %>
          </td>

          <td>
            <%= link_to(
              'Edit',
              edit_sub_genre_path(sub_genre),
              class: 'btn btn-sm btn-outline-success'
            ) %>
          </td>

          <td>
            <%= link_to(
              'Delete',
              sub_genre,
              method: :delete,
              data: { confirm: 'Are you sure?' },
              class: 'btn btn-sm btn-outline-danger'
            ) %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
